class Util {
    static clamp(value, min, max) {
        return Math.min(Math.max(value, min), max);
    }
    static limitToNormal(x, y, max) {
        return Math.min(Math.max(value, min), max);
    }
}